<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="utf-8"/>
<meta content="width=device-width, initial-scale=1.0" name="viewport"/>
<title>Famille Rascalov - Table du 22/12/25</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Roboto:wght@300;400;700&display=swap" rel="stylesheet"/>
<style>
* { margin: 0; padding: 0; box-sizing: border-box; }
body { 
    font-family: 'Roboto', sans-serif; 
    background: #0a0a0a; 
    color: #e0e0e0; 
    overflow-x: hidden;
}

/* HERO */
.hero { 
    position: relative; 
    width: 100%; 
    height: 100vh; 
    overflow: hidden; 
    display: flex; 
    align-items: center; 
    justify-content: center; 
}
.hero-bg { 
    position: absolute; 
    top: 0; 
    left: 0; 
    width: 100%; 
    height: 100%; 
    z-index: 1; 
}
.hero-bg img { 
    width: 100%; 
    height: 100%; 
    object-fit: cover; 
    filter: contrast(1.1) brightness(0.6) blur(3px); 
}
.hero-overlay { 
    position: absolute; 
    top: 0; 
    left: 0; 
    width: 100%; 
    height: 100%; 
    background: linear-gradient(180deg, rgba(0,0,0,0.7) 0%, rgba(139,0,0,0.2) 50%, rgba(0,0,0,0.95) 100%); 
    z-index: 2; 
}
.hero-content {
    position: relative;
    z-index: 3;
    text-align: center;
    padding: 40px;
}
.hero-title {
    font-family: 'Playfair Display', serif;
    font-size: 72px;
    color: #8b0000;
    text-shadow: 0 0 30px rgba(139,0,0,0.8), 0 0 60px rgba(139,0,0,0.4);
    letter-spacing: 8px;
    margin-bottom: 20px;
    animation: heroGlow 3s ease-in-out infinite;
}
@keyframes heroGlow {
    0%, 100% { text-shadow: 0 0 30px rgba(139,0,0,0.8), 0 0 60px rgba(139,0,0,0.4); }
    50% { text-shadow: 0 0 50px rgba(139,0,0,1), 0 0 100px rgba(139,0,0,0.6); }
}
.hero-subtitle {
    font-size: 24px;
    color: #c06060;
    letter-spacing: 4px;
    text-transform: uppercase;
}
.scroll-indicator { 
    position: absolute; 
    bottom: 30px; 
    left: 50%; 
    transform: translateX(-50%); 
    color: rgba(139,0,0,0.8); 
    font-size: 12px; 
    text-transform: uppercase; 
    letter-spacing: 3px; 
    animation: bounce 2s infinite; 
    z-index: 10; 
}
.scroll-indicator::after { 
    content: ''; 
    display: block; 
    width: 1px; 
    height: 40px; 
    background: linear-gradient(180deg, rgba(139,0,0,0.8) 0%, transparent 100%); 
    margin: 10px auto 0; 
}
@keyframes bounce {
    0%, 100% { transform: translateX(-50%) translateY(0); }
    50% { transform: translateX(-50%) translateY(-10px); }
}

/* CONTAINER PRINCIPAL */
.container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 80px 40px;
    position: relative;
    z-index: 5;
}

/* EFFET TABLE - FUMÉE */
.smoke-container { 
    position: fixed; 
    top: 0; 
    left: 0; 
    width: 100%; 
    height: 100%; 
    overflow: hidden; 
    pointer-events: none;
    z-index: 1;
}
.smoke { 
    position: absolute; 
    border-radius: 50%; 
    background: radial-gradient(ellipse at center, rgba(80,80,100,0.12) 0%, transparent 70%); 
    animation: smokeFloat ease-in-out infinite; 
    filter: blur(40px); 
}
@keyframes smokeFloat {
    0%, 100% { transform: translateY(0) translateX(0) scale(1); opacity: 0.3; }
    25% { transform: translateY(-40px) translateX(30px) scale(1.15); opacity: 0.5; }
    50% { transform: translateY(-60px) translateX(-20px) scale(1.25); opacity: 0.4; }
    75% { transform: translateY(-30px) translateX(40px) scale(1.1); opacity: 0.5; }
}

/* EN-TÊTE DOCUMENT */
.doc-header {
    background: linear-gradient(135deg, rgba(139,0,0,0.15) 0%, rgba(0,0,0,0.6) 100%);
    border: 2px solid rgba(139,0,0,0.3);
    border-left: 6px solid #8b0000;
    padding: 40px 50px;
    margin-bottom: 60px;
    position: relative;
    border-radius: 4px;
    box-shadow: 0 10px 40px rgba(139,0,0,0.2);
}
.doc-header::before {
    content: '🔒';
    position: absolute;
    top: 20px;
    right: 30px;
    font-size: 32px;
    opacity: 0.3;
}
.doc-classification {
    display: inline-block;
    background: rgba(139,0,0,0.3);
    color: #ff4d4d;
    padding: 8px 20px;
    border: 1px solid #8b0000;
    border-radius: 3px;
    font-size: 12px;
    letter-spacing: 3px;
    text-transform: uppercase;
    font-weight: 700;
    margin-bottom: 20px;
}
.doc-title {
    font-family: 'Playfair Display', serif;
    font-size: 42px;
    color: #ff6666;
    margin-bottom: 15px;
    letter-spacing: 3px;
}
.doc-subtitle {
    font-size: 18px;
    color: #c08080;
    letter-spacing: 2px;
    margin-bottom: 25px;
}
.doc-meta {
    display: flex;
    gap: 30px;
    font-size: 14px;
    color: #909090;
    margin-top: 20px;
    padding-top: 20px;
    border-top: 1px solid rgba(139,0,0,0.2);
}
.doc-meta span {
    display: flex;
    align-items: center;
    gap: 8px;
}

/* SECTIONS */
.section {
    background: linear-gradient(135deg, rgba(139,0,0,0.08) 0%, rgba(0,0,0,0.5) 100%);
    border: 1px solid rgba(139,0,0,0.2);
    border-left: 4px solid #8b0000;
    padding: 40px 50px;
    margin-bottom: 40px;
    border-radius: 4px;
    transition: all 0.4s ease;
}
.section:hover {
    border-color: rgba(139,0,0,0.5);
    box-shadow: 0 0 40px rgba(139,0,0,0.15);
    transform: translateX(5px);
}
.section-icon {
    font-size: 32px;
    margin-bottom: 20px;
}
.section-title {
    font-family: 'Playfair Display', serif;
    font-size: 28px;
    color: #ff4d4d;
    margin-bottom: 25px;
    letter-spacing: 2px;
    display: flex;
    align-items: center;
    gap: 15px;
}
.section-content {
    font-size: 17px;
    line-height: 1.8;
    color: #c0c0c0;
}
.section-content p {
    margin-bottom: 20px;
}
.section-content strong {
    color: #ff6666;
    font-weight: 700;
}

/* LISTES */
.list-styled {
    list-style: none;
    padding-left: 0;
    margin: 25px 0;
}
.list-styled li {
    position: relative;
    padding-left: 35px;
    margin-bottom: 15px;
    font-size: 17px;
    line-height: 1.7;
    color: #b0b0b0;
    transition: all 0.3s ease;
}
.list-styled li::before {
    content: '▸';
    position: absolute;
    left: 0;
    color: #8b0000;
    font-weight: bold;
    font-size: 18px;
    transition: all 0.3s ease;
}
.list-styled li:hover {
    color: #e0e0e0;
    padding-left: 40px;
}
.list-styled li:hover::before {
    color: #ff4d4d;
    text-shadow: 0 0 10px rgba(255,77,77,0.8);
}

/* HIGHLIGHT BOX */
.highlight-box {
    background: linear-gradient(135deg, rgba(139,0,0,0.2) 0%, rgba(50,0,0,0.4) 100%);
    border: 2px solid rgba(139,0,0,0.4);
    padding: 25px 35px;
    margin: 30px 0;
    border-radius: 4px;
    position: relative;
}
.highlight-box::before {
    content: '⚠️';
    position: absolute;
    top: -15px;
    left: 30px;
    font-size: 24px;
    background: #0a0a0a;
    padding: 0 10px;
}
.highlight-title {
    font-size: 20px;
    color: #ff6666;
    margin-bottom: 15px;
    font-weight: 700;
    letter-spacing: 1px;
}

/* CONCLUSION */
.conclusion {
    background: linear-gradient(135deg, rgba(139,0,0,0.15) 0%, rgba(0,0,0,0.7) 100%);
    border: 2px solid rgba(139,0,0,0.4);
    border-left: 6px solid #8b0000;
    padding: 40px 50px;
    margin-top: 60px;
    border-radius: 4px;
    text-align: center;
}
.conclusion-title {
    font-family: 'Playfair Display', serif;
    font-size: 32px;
    color: #ff6666;
    margin-bottom: 20px;
    letter-spacing: 3px;
}
.conclusion-text {
    font-size: 18px;
    line-height: 1.8;
    color: #b0b0b0;
    margin-bottom: 15px;
}
.conclusion-stamp {
    margin-top: 40px;
    padding-top: 30px;
    border-top: 1px solid rgba(139,0,0,0.3);
}
.stamp-text {
    font-size: 14px;
    color: #808080;
    text-transform: uppercase;
    letter-spacing: 3px;
}

/* FOOTER */
.footer {
    text-align: center;
    padding: 60px 40px;
    border-top: 1px solid rgba(139,0,0,0.2);
}
.footer-logo {
    font-family: 'Playfair Display', serif;
    font-size: 32px;
    color: #8b0000;
    letter-spacing: 6px;
    margin-bottom: 20px;
}
.footer-text {
    font-size: 14px;
    color: #606060;
    letter-spacing: 2px;
}

/* RESPONSIVE */
@media (max-width: 768px) {
    .hero-title { font-size: 42px; }
    .container { padding: 40px 20px; }
    .doc-header, .section, .conclusion { padding: 25px 20px; }
    .doc-title { font-size: 32px; }
    .section-title { font-size: 22px; }
}
</style>
</head>
<body>

<!-- HERO -->
<section class="hero">
    <div class="hero-bg">
        <img alt="Rascalov Table" src="https://files.catbox.moe/pux2fr.png"/>
    </div>
    <div class="hero-overlay"></div>
    <div class="hero-content">
        <h1 class="hero-title">ARCHIVES</h1>
        <p class="hero-subtitle">Comptes Rendus de la Table 22/12/25</p>
    </div>
    <div class="scroll-indicator">Accéder au rapport</div>
</section>

<!-- FUMÉE -->
<div class="smoke-container" id="smokeContainer"></div>

<!-- CONTAINER PRINCIPAL -->
<div class="container">
    
    <!-- EN-TÊTE DOCUMENT -->
    <div class="doc-header">
        <span class="doc-classification">🔒 CONFIDENTIEL</span>
        <h1 class="doc-title">RASCALOV : TABLE</h1>
        <h2 class="doc-subtitle">Compte rendu tenue le 22/12/25</h2>
        <div class="doc-meta">
		    <span>📅 Date : 22/12/25</span>
            <span>📁 Classification : Interne uniquement</span>
            <span>📌 Statut : Archivé</span>
        </div>
    </div>

    <!-- INTRODUCTION -->
    <div class="section">
        <div class="section-icon">🪑</div>
        <h3 class="section-title">Ouverture de Table</h3>
        <div class="section-content">
            <p>La table s'est tenue dans une ambiance <strong>calme et détendue</strong>, malgré un effectif réduit et un léger retard. La priorité a été donnée à l'efficacité, avec une volonté claire de lancer rapidement les actions prévues pour la soirée.</p>
        </div>
    </div>

    <!-- POINTS DE SITUATION -->
    <div class="section">
        <div class="section-icon">🤝</div>
        <h3 class="section-title">Points de Situation et Informations en Cours</h3>
        <div class="section-content">
            <p>Il a été annoncé que les <strong>Russes</strong> ont repris contact et devraient confirmer autour de <strong>22h10</strong> leur intention d'acheter <strong>500 pochons</strong>, sous réserve des quantités disponibles. La situation reste en attente de confirmation définitive.</p>
            
            <div class="highlight-box">
                <div class="highlight-title">No Flag Mob</div>
                <p>Il a également été mentionné que les <strong>No Flag Mob</strong> pourraient entretenir des relations étroites avec les Russes.</p>
                <p>Aucune décision immédiate n'a été prise à leur sujet, mais un appel est prévu afin de clarifier leur position, après les premiers rendez-vous de la soirée.</p>
            </div>
        </div>
    </div>

    <!-- TERRITOIRES -->
    <div class="section">
        <div class="section-icon">🗺️</div>
        <h3 class="section-title">Territoires et Revendications</h3>
        <div class="section-content">
            <p>La stratégie territoriale de la soirée a été précisée :</p>
            
            <ul class="list-styled">
                <li><strong>Université</strong> et <strong>Vinewood</strong> sont ciblés en priorité</li>
                <li>Si les Families ne sont pas présents en ville, ces zones seront revendiquées</li>
                <li>Shakin a été chargé de vérifier leur présence, sans réponse à ce stade</li>
            </ul>

            <p>Il a été rappelé que toute revendication devra être <strong>annoncée clairement après validation</strong>, et suivie par une présence régulière sur zone.</p>
        </div>
    </div>

    <!-- RENCONTRES PRÉVUES -->
    <div class="section">
        <div class="section-icon">🤝</div>
        <h3 class="section-title">Rencontres Prévues</h3>
        <div class="section-content">
            <p>Les actions prévues pour le début de soirée ont été organisées comme suit :</p>
            
            <div class="highlight-box">
                <div class="highlight-title">Hooligans</div>
                <p>Un rendez-vous est prévu rapidement. Il a été confirmé qu'ils sont favorables aux discussions engagées la veille.</p>
                <p><strong>Objectif :</strong> poser des bases claires de collaboration.</p>
            </div>

            <div class="highlight-box">
                <div class="highlight-title">Gitans</div>
                <p>Un simple échange de confirmation est suffisant.</p>
                <p>Si les conditions leur conviennent et qu'ils se sentent capables de travailler sur les zones concernées, les territoires seront revendiqués dans la foulée.</p>
            </div>

            <div class="highlight-box">
                <div class="highlight-title">Families</div>
                <p>Une tentative de contact est en cours afin de vérifier leur présence en ville et l'état de leur activité.</p>
            </div>
        </div>
    </div>

    <!-- COCAÏNE -->
    <div class="section">
        <div class="section-icon">💊</div>
        <h3 class="section-title">Cocaïne : Production et Organisation</h3>
        <div class="section-content">
            <p>Un point précis a été fait sur les <strong>stocks disponibles</strong>. Les quantités actuelles permettent de produire un volume suffisant pour répondre aux demandes à court terme, en privilégiant la qualité.</p>
            
            <div class="highlight-box">
                <div class="highlight-title">Décision prise :</div>
                <ul class="list-styled">
                    <li>Lancer une session de découpe en attendant les retours des Russes</li>
                    <li>Rester exclusivement sur du <strong>produit pur</strong></li>
                    <li>Adapter la production selon les commandes confirmées</li>
                </ul>
            </div>
        </div>
    </div>

    <!-- ORGANISATION INTERNE -->
    <div class="section">
        <div class="section-icon">🏗️</div>
        <h3 class="section-title">Organisation Interne et Projets</h3>
        <div class="section-content">
            <p>Il a été confirmé que la mise en place du <strong>système de pôles</strong> est en cours de finalisation. Deux pôles seront priorisés dans un premier temps :</p>
            
            <ul class="list-styled">
                <li>Pôle cocaïne / drogue</li>
                <li>Pôle sécurité</li>
            </ul>

            <p>La mise en place est prévue <strong>dès le lendemain</strong>, sous réserve des derniers ajustements.</p>

            <div class="highlight-box">
                <div class="highlight-title">Entraînements physiques</div>
                <p>Un projet d'entraînements physiques réguliers a également été évoqué : des sessions courtes, d'environ <strong>10 minutes par jour</strong>, afin d'améliorer l'endurance et la condition générale des membres.</p>
            </div>
        </div>
    </div>

    <!-- OBSERVATION -->
    <div class="section">
        <div class="section-icon">👀</div>
        <h3 class="section-title">Observation et Renseignement</h3>
        <div class="section-content">
            <p>Une proposition a été validée : mettre en place des <strong>équipes d'observation</strong> sur différents secteurs afin de :</p>
            
            <ul class="list-styled">
                <li>Identifier l'activité réelle sur les terrains</li>
                <li>Vérifier qui occupe quelles zones</li>
                <li>Confirmer ou infirmer certaines informations reçues</li>
            </ul>

            <p><strong>Les secteurs évoqués incluent notamment :</strong></p>
            <ul class="list-styled">
                <li>La plage</li>
                <li>Les zones foraines</li>
                <li>Le sud de la ville</li>
            </ul>
        </div>
    </div>

    <!-- CONCLUSION -->
    <div class="conclusion">
        <div class="section-icon">📌</div>
        <h3 class="conclusion-title">Conclusion</h3>
        <p class="conclusion-text">La table a permis de poser une ligne claire pour la soirée :</p>
        <ul class="list-styled">
            <li>Contacts extérieurs structurés</li>
            <li>Priorités territoriales définies</li>
            <li>Production lancée de manière contrôlée</li>
            <li>Organisation interne en évolution</li>
            <li>Vigilance accrue sur les mouvements en ville</li>
        </ul>
        
        <div class="conclusion-stamp">
            <p class="stamp-text">Dossier archivé</p>
        </div>
    </div>

</div>

<!-- FOOTER -->
<div class="footer">
    <div class="footer-logo">RASCALOV</div>
    <p class="footer-text">Archives Table : Confidentiel</p>
</div>

<script>
// Création de la fumée
function createSmoke() {
    const container = document.getElementById('smokeContainer');
    for (let i = 0; i < 18; i++) {
        const smoke = document.createElement('div');
        smoke.className = 'smoke';
        smoke.style.left = Math.random() * 100 + '%';
        smoke.style.top = Math.random() * 100 + '%';
        smoke.style.width = (120 + Math.random() * 250) + 'px';
        smoke.style.height = smoke.style.width;
        smoke.style.animationDuration = (10 + Math.random() * 15) + 's';
        smoke.style.animationDelay = Math.random() * 8 + 's';
        container.appendChild(smoke);
    }
}
createSmoke();

// Parallaxe hero
window.addEventListener('scroll', () => {
    const scrolled = window.pageYOffset;
    const heroImg = document.querySelector('.hero-bg img');
    if (heroImg) heroImg.style.transform = `translateY(${scrolled * 0.5}px)`;
});
</script>

</body>
</html>
